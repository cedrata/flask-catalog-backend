[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
python-dotenv = ">=1.0.1"
flask = ">=3.0.3"
prizes-flask-api = {file = ".", editable = true}

[dev-packages]
pytest=">=8.2.2"

[requires]
python_version = "3.12"
python_full_version = "3.12.3"

[scripts]
tests-execution = "python -m pytest -v"
tests-list = "python -m pytest --co"
